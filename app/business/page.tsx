import Link from "next/link";
import { ArrowUpRight, BriefcaseBusiness, ChartNoAxesCombined, Users } from "lucide-react";
import { employerDemo, formatTenge } from "@/lib/demoData";
import AppShell from "@/components/app/AppShell";

export default function BusinessPage() { return <AppShell><div><div className="mb-10"><p className="eyebrow mb-4">Business workspace</p><h1 className="text-[48px] font-extrabold leading-none tracking-[-0.04em]">Your payroll, in context.</h1><p className="mt-4 max-w-[560px] text-[16px] leading-relaxed text-ink-soft">A foundation for employee access, payroll visibility and financial wellbeing insights.</p></div><div className="grid gap-5 md:grid-cols-3">{[["Employees", employerDemo.employees.toLocaleString("en-US"), Users], ["Earned payroll", formatTenge(employerDemo.earnedPayroll), BriefcaseBusiness], ["Active this month", employerDemo.activeThisMonth.toLocaleString("en-US"), ChartNoAxesCombined]].map(([label, value, Icon]) => <section key={label as string} className="rounded-[20px] border border-ink/[0.08] bg-white/65 p-5"><Icon size={19} className="text-orange" /><p className="mt-5 text-[12px] uppercase tracking-[0.1em] text-ink-soft">{label as string}</p><p className="mt-2 text-[30px] font-extrabold">{value as string}</p></section>)}</div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Employees", "/business/employees"], ["Payroll", "/business/payroll"], ["Analytics", "/business/analytics"], ["Requests", "/business/requests"]].map(([item, href]) => <Link key={item} href={href} className="flex items-center justify-between rounded-[16px] border border-ink/[0.08] bg-cream-orange p-5 text-[15px] font-bold">{item}<ArrowUpRight size={17} /></Link>)}</div></div></AppShell>; }
